{% extends 'base.html.twig' %}

{% block title %}Edit Proveedor{% endblock %}

{% block body %}
    <h1>Edit Proveedor</h1>

    {{ include('proveedor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('proveedor_index') }}">back to list</a>

    {{ include('proveedor/_delete_form.html.twig') }}
{% endblock %}
